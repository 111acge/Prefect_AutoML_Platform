# Prefect AutoML Platform 环境变量示例

# 数据库（默认 SQLite）
DATABASE_URL=sqlite+aiosqlite://./data/db.sqlite

# LLM 意图解析（可选；未配置时自动降级到规则引擎）
# 支持 provider：kimi / deepseek / minimax / openai / auto
LLM_PROVIDER=auto

KIMI_API_KEY=
DEEPSEEK_API_KEY=
MINIMAX_API_KEY=
OPENAI_API_KEY=

# 覆盖默认模型（留空则使用各提供商默认轻量模型）
DEFAULT_LLM_MODEL=
