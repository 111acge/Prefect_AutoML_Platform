# Web Framework
fastapi>=0.111.0,<0.115.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# Data Validation & Settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
aiosqlite>=0.20.0

# Workflow Orchestration
prefect>=3.0.0,<4.0.0

# AutoML (full version, includes torch/fastai/NN)
autogluon>=1.1.0

# PyTorch (CUDA version from PyPI; install CPU-only via requirements-core.txt if needed)
torch>=2.0.0

# Data Science Core
pandas>=2.0.0,<2.4.0
numpy>=1.25.0,<2.3.0
scikit-learn>=1.4.0,<1.7.0
pyarrow>=14.0.0,<20.0.0

# File I/O
openpyxl>=3.1.0
xlrd>=2.0.0

# Feature Engineering & Sampling
feature-engine>=1.6.0
imbalanced-learn>=0.11.0

# Explainability
shap>=0.44.0

# Optional extra deep learning models for tabular AutoML
# (enabled automatically at runtime when packages are installed)
fastai>=2.7.0
tabpfn>=0.1.0

# LLM API (OpenAI-compatible providers: KIMI / DeepSeek / MiniMax)
openai>=1.0.0

# Serialization & Reporting
joblib>=1.3.0
jinja2>=3.1.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
packaging>=24.0
